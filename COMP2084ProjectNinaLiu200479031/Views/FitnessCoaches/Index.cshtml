@model IEnumerable<COMP2084ProjectNinaLiu200479031.Models.FitnessCoach>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CoachName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Major)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Introduction)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PhotoLink)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CoachName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Major)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Introduction)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PhotoLink)
            </td>
            <td>
               if (this.User.IsInRole("Administrator")) { 
                <a asp-action="Edit" asp-route-id="@item.CoachID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CoachID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CoachID">Delete</a>
                       } 
            </td>
        </tr>
}
    </tbody>
</table>
